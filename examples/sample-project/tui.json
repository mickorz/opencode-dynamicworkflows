{
  "$schema": "https://opencode.ai/config.json",
  // TUI 侧插件配置（F-20 sidebar 实时树）：与 opencode.json 分离，TUI 进程单独读取
  // 同一包路径，经 package.json 的 exports["./tui"] 加载 src/tui/index.tsx（bun 直读 TSX 源码，无需 build）
  // 相对路径基准 = 本配置文件所在目录（config/plugin.ts:43-53），../.. 即仓库根
  "plugin": ["../.."]
}
